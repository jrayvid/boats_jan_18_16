<h1>this is the edit page for Users</h1>

<%= form_for @user do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :username %>
	<%= f.text_field :username %>
	<%= f.label :password %>
	<%= f.text_field :password %>
	<%= f.label :Picture %>
	<%= f.file_field :avatar%> 
	
	<%= form_for @user, :url => users_path,
	:html => { :mulitpart => true } do |form| %>
	<%= form.file_field :avatar %>
	

	<%= f.submit %>
<% end %>
<% end %>